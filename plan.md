# CASHWMaxSAT + SPBMAXSAT2 混合算法实施与实验计划

## 1. 问题与判定

输入是 WCNF。目标为最小化未满足软子句的总权重。CASH 保持精确求解状态与 LB；SPB 在完整模型上搜索，并在每个 3 秒窗口末输出本轮找到的最佳 UB。SPB 不单独证明最优。若其 UB 被 CASH 接收并最终由 CASH 得到 `LB = UB`，该实例计为已证明最优。

## 2. 固定调度

```text
初始化一个 CASH 和一个 SPB 实例
while CASH 未证明最优且尚可启动下一模块：
    CASH 从当前状态运行 15 秒
    若总预算在该窗口内到达：完成该窗口并停止
    在安全检查点暂停 CASH
    导出仍有效的原变量传播推理值
    用导出值 + 新随机未定变量初始化 SPB
    SPB 保留累计子句权重，运行完整 3 秒
    取得本轮最佳数值 UB
    若 UB 严格改善 CASH 当前 UB：由最小回调交给 CASH
    从原暂停状态恢复 CASH
    若总预算在 SPB 中到达：完整运行该 SPB 轮，交 UB 一次后停止
```

SPB 可翻转用于初始化的推理变量，但其赋值永不写回 CASH。每个实例的 SPB 对象和自适应子句权重跨轮保留；每轮变量初值不保留。协同层不重复验证 SPB 的模型，也不制定 CASH 的硬化规则；CASH 根据其自身实现处理收到的数值 UB。

### 2.1 语义保护

SPB 对 CASH 的公开结果只有数值 UB，但 SPB 封装在内部必须保存产生该 UB 的完整模型，并按 Parser Gate 通过后的原始 WCNF 语义检查硬可行性和目标值相等。这样并不把模型回写给 CASH；它只阻止错误的数值 UB 进入精确求解。

不能把原始 WCNF UB 直接写入 CASH 的 `best_goalvalue`、`UB_goalvalue` 或 `LB_goalvalue`。CASH 在松弛、固定代价与最大公约数缩放后使用不同的内部目标单位。必须在目标变换完成后提供一个经过单元测试的唯一适配入口，由该入口进行单位转换并调用 CASH 自身的界收紧路径；外部 UB 不得改变 LB，也不得自行硬化子句。最终若需要输出模型，只能输出 CASH 自己的模型或 SPB 封装私有保存并已核对的证书模型。

## 3. 目录和模块

在项目根目录建立：

```text
HybridMaxSAT/
  CMakeLists.txt
  include/                 # 协同回调与数据结构
  src/                     # 调度器、SPB 驱动、CASH 适配器
  tests/                   # Parser Gate 与协议测试
  configs/                 # 调试/正式实验配置
  manifests/               # 实例清单和抽样记录
  scripts/                 # 构建、运行、汇总脚本
  runs/                    # 不纳入版本库的大型日志和结果
```

保持 `CASHWMaxSAT-DisjCad-S6` 与 `SPBMAXSAT2-master` 相互独立。CASH 只增加安全点回调、原变量传播快照导出和数值 UB 接收接口。SPB 通过库接口运行，以得到每个窗口的最佳 UB，并确保一个对象跨窗口保留自适应子句权重。

## 4. Gate 0：修复 SPB WCNF 解析

已复现的缺陷是：SPB 在标准 `p wcnf` 头读入前分配内存，并对 `h` 子句和 `top` 的硬子句使用不一致语义，导致其在 CASH 自带样例上报出不可信的 `bestcost 0`，而 CASH 精确结果为 1611。

实施顺序：

1. 先读并校验唯一的 `p wcnf <nvars> <nclauses> <top>` 头，再分配数组。
2. 规范化数值 `top` 权重和 `h` 子句的硬子句表示；原始软权重使用 64 位整数保留。
3. 检查子句数量、文字范围和 `0` 终止符；错误应清晰失败。
4. 写小型 WCNF 回归测试，覆盖软权重、硬子句、不可满足硬约束和格式错误。
5. 以 CASH 已知小实例与自带样例交叉检查成本语义。

还必须测试外部 UB 桥接：正常的严格改善 UB、相等/变差 UB、故意过小 UB、含固定代价的实例、权重存在公约数的实例，以及触发 CASH 硬化的实例。每例都应检查原目标、内部目标单位、LB、UB 与最终模型一致。

通过 Gate 0 前，禁止开始 50 实例调试批次。

## 5. 调试实验

数据来自只读服务器路径 `/data/dataset/Maxsat/Complete/MSE23W`。使用种子 `20260909` 在所有 `.wcnf` 中均匀抽取 50 个。解析失败候选须记录并补抽；最终清单必须被版本控制。

每个实例使用完整 600 秒端到端预算，运行以下配置：

| 配置 | 行为 |
|---|---|
| CASH | 原始 CASH 连续运行 600 秒 |
| SPB | 独立 SPB 连续运行 600 秒 |
| Hybrid | 15 秒 CASH / 3 秒 SPB 的暂停恢复协同 |
| Hybrid-NoInference | 同 Hybrid，但不把 CASH 推理值用于 SPB 初始解 |

若调试批次任何运行出现崩溃、解析错误、非法界值或日志不一致，立即停止整个批次；修复后从头重跑。批次全部完成后，先生成逐实例报告，供用户检查后再进行全量实验。

## 6. 正式实验

调试验收后，对 `MSE23W` 和 `MSE24W` 全部实例运行上述四种配置。每个实例、每种配置运行三次，重复种子为 `20260909`、`20260910`、`20260911`。相同实例和重复在 SPB、Hybrid、Hybrid-NoInference 中派生同一随机种子。

主要结果是“3 次中由 CASH 证明最优的次数”。逐次保留端到端耗时；并记录最终 LB 与 UB，作为解释性数据。独立 SPB 的 600 秒结果只作基线，不能标为证明最优。

## 7. 输出与验收

每个运行生成结构化结果和逐轮 JSONL 事件。字段至少包括：实例相对路径、配置、种子、开始/结束时间、轮次、CASH 运行时间、传播变量数量、轮前 CASH UB、SPB 最佳 UB、CASH 接收后 UB、最终 LB、最终 UB、CASH 最优性状态、退出原因。

调试报告应有 50 个实例的状态表，逐项标明 CASH、SPB、Hybrid、Hybrid-NoInference 是否正常完成以及 CASH 是否证明最优。正式报告在每种配置下统计三次成功数和时间，不只汇报最好一次。

## 8. 当前状态

服务器 S122 当前 SSH 超时。文件在本地 Git 镜像中编写并推送 GitHub；连接恢复后，同步到 `/home/fwkzj/HybridAlgorithm`，在服务器执行构建与实验。
