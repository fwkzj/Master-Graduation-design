# design.md：三条改进主线的设计与实验指引

> **本文件的性质**：后续实验的入口级设计文件。它把「接下来改什么、为什么改、怎么证伪」收敛到三条主线上，
> 每条主线都给出当前实现、已有证据、候选方案、判定阈值与建议批次。过程记录仍写在
> `docs/experiments/<日期>-<主题>.md`，本文件只保留结论、判据与状态，避免变成日志堆积。
>
> **权威顺序**：算法与正确性约束以 `agent.md` 为准；实验执行与输入输出契约以 `experiment.md` 为准；
> 阶段划分以 `plan.md` 为准。本文件若与它们冲突，以上述三份为准。
>
> **状态基准**：2026-09-12，服务器 `S122:/home/fwkzj/HybridAlgorithm`，HEAD `97ae1e7`。
> 本文所有数字都可以在 `docs/experiments/` 与 `runs/` 中按批次名回溯。

---

## 0. 速览

| 编号 | 改进点 | 一句话目标 | 当前实现 | 判定状态 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| **D1** | 何时启动 SPB 与精确求解器配合 | 只在 SPB 可能改变精确搜索时才交接 | 固定 30/10 → `HybridSelective` → `HybridLate` → `HybridGate` | 省成本已成立（SPB 耗时 −70%），证明数未追平纯 CASH | `full_gate_phase` 全量验证；再做进展触发与末段收尾 |
| **D2** | SPB 窗口的终止条件 | 用「目标 + 时间上限 + 分轮次」替代「跑满窗口」 | `cutoff_time = min(窗口, 剩余预算)`；目标 = 下一条硬化阈值（A1） | 窗口浪费已被量化（拿到最好解后中位空跑 6.07 s）；目标几乎不命中 | 窗口档位 3/5/10 s 对照；目标档位对照 |
| **D3** | 启发式的子句分层方案 | 把层边界切在信息量最大的位置 | 几何对折（P0）；P1–P4、S2 已实现 | 语义 Gate 通过；P2 上界收益两种子稳定，但不迁移到混合 | 特征选择器 / 在线控制器；P2 定位为「解质量优先」配置 |

三条主线的分工：

- **D1 决定「要不要付」**这次交接的代价。
- **D2 决定「付多少」**——一个 SPB 窗口该跑多长、以什么为提前结束条件。
- **D3 改变精确求解器内部的搜索组织**，与 D1/D2 独立，且已验证不与混合协议叠加使用。

### 0.1 统一口径（所有实验都必须遵守）

- **主指标**：`cash_proved_optimal == true` 的运行数（「由 CASH 精确推理证明 `LB = UB`」）。
  独立 SPB 永不证明最优；`exit_reason=optimum_scip` 必须单独计数，不能与 CDCL 推出的 `optimum` 混算。
- **副指标**：末态 `final_ub` 的逐实例配对（更优 / 持平 / 更差），以及 SPB 总耗时、交接轮次、被接受轮次。
- **机制指标**：`harden_count`、`harden_gap_to_next`、`harden_goal_interval`、逐轮 `lb/ub` 轨迹、
  `spb_steps` / `spb_tries` / `spb_init_seconds` / `spb_setup_seconds` / `spb_best_at_seconds`。
- **可复现性**：每批次 `meta.json` 记 `git_commit` 与 `binary_sha1`；工作树脏时产出的结果不采信；
  总并发度不超过 64（见 `agent.md`）。
- **正确性红线**（`agent.md`「变换正确性：不可放宽的约束」）：SPB 对 CASH 只输出数值 UB；
  禁止直接写 `best_goalvalue` / `UB_goalvalue` / `LB_goalvalue`；外部 UB 永不单独宣布最优。
  本文件提出的任何目标值、相位、分层改动都不得越过这条线。

### 0.2 当前机器状态（写本文件时）

| 项目 | 状态 |
| --- | --- |
| HEAD | `97ae1e7`（阈值闸门 + 相位注入 + A1 提前退出修复） |
| 正在运行 | `runs/full_gate_phase`（`CASH,HybridGate,HybridPhase`，558 实例，SPB 5 s，64 并发） |
| 刚完成、尚未写入实验记录 | `runs/full_a1_cash_late`（`CASH,HybridLate`，558×1，已完成 1116/1116） |
| 数据不完整 | `runs/clean_strat_p0/p2` 只跑了 77 / 89 次即被中断，回调快照缓存（`ef97202`）的收益尚无可引用结论 |
| 数据集限制 | `MSE24W` 目录属主为 `caiyujie`、当前不可读，只能用 `MSE23W`（558 实例） |

---

## D1：什么时候启动 SPB，与精确求解器配合求解

### D1.1 设计问题

SPB 的边际收益按轮次急剧衰减，而每次交接都要付三项成本：

1. **时间税**：CASH 让出一个窗口（当前 10 s，约占墙钟 25%）；
2. **轨迹打断**：抢占式交接把 CaDiCaL 从一次 SAT 调用中间拉出来，它回不到原搜索轨迹；
3. **UB 注入的副作用**：UB 骤降会让 `UB − LB` 一次性跨过多个权重阈值、硬化过多软子句，可能过度约束搜索。

因此「何时交接」不是调度细节，而是混合框架的主要性能变量。要回答的是三件事：
首轮何时启动、后续按什么节奏触发、以及停止之后是否还要在预算末段收尾一次。

### D1.2 当前实现（HEAD `97ae1e7`）

| 配置 | 交接策略 |
| --- | --- |
| `Hybrid` | 抢占式固定窗口：CASH 30 s / SPB 10 s，无条件轮转。**不再作为正式配置** |
| `HybridNatural` | 非抢占：终结器只挂总预算，SPB 等 CASH 自己到达调度点 |
| `HybridAdaptive` | 非抢占 + 基于进展的窗口退避 |
| `HybridSelective` | 首轮 30 s 必试；上轮被接受→保持 30 s 节奏；落空→退避到 120 s 重试一次；再落空→停止交接，把剩余预算还给 CASH |
| `HybridLate` | 选择性 + 首轮推迟到预算的 50% |
| `HybridGate` | 非抢占 + 首轮推迟到 30% + **阈值闸门**：只有 `harden_gap_to_next / UB ≤ gate_percent(=5%)` 时才交接 |

相关开关：`HybridSchedule::selective` / `start_fraction` / `threshold_gate` / `gate_percent` /
`preemptive`，以及每轮事件里的 `schedule_reason`（`continue_after_gain` / `retry_after_miss` /
`stopped_after_two_misses`）与 `next_handoff_in`。

### D1.3 已有证据

**（a）轮次收益急剧衰减**（`runs/full23w_fix`，2557 个真实交接轮次）：

| 上一轮状态 | 下一轮被接受概率 |
| --- | --- |
| 上轮被接受 | **32.8%**（112/341） |
| 上轮可行但未改善 | 4.6%（59/1274） |
| 上轮不可行（SPB 无解） | 1.8%（12/683） |
| 连续两轮未改善 | 2.2%（37/1672） |

| 轮次 | 接受率 |
| --- | --- |
| 第 1 轮 | **69.1%**（179/259） |
| 第 2~3 轮 | 19.0% |
| 第 4~7 轮 | 9.2% |
| 第 8 轮以后 | 2.6% |

**（b）选择式与延后启用只省成本、不补回证明数**：

| 批次与对比 | 证明最优 | 交接轮次 | SPB 总耗时 |
| --- | --- | --- | --- |
| `debug50_selective`：固定 `Hybrid` → `HybridSelective` | 36 → 36（/50） | 240 → 66（−72%） | 2394.6 → 664.1 s（−72%） |
| `full23w_selective`：固定 `Hybrid` → `HybridSelective` | 386 → **388**（/558） | 2557 → 763（−70%） | 25406.5 → 7666.8 s（−70%） |
| 同上，相对纯 `CASH` | 395 → 388（**−7**） | — | — |
| `a1_debug50`：`HybridNatural` → `HybridLate` | 35 → **36**（/50） | 114 → 44 | 1142.4 → 441.4 s（**−61%**） |

**（c）第二个损失来源：注入 UB 之后 CASH 自己提前结束。**
墙钟 < 400 s、未证明、`exit_reason=budget` 的运行数：
`CASH` 3/558、`HybridSelective` 8/558、固定 `Hybrid` 17/558。也就是说交接不仅收时间税，
还会让 CASH 主循环更容易在 `LB < UB` 时判定「搜完了」。

**（d）停止交接会带来尾部事故。** `relational-inference_wt-pa-2` 因「首轮未中即停止」放弃后续本可拿到的改善，
末态 UB 比 CASH 差 67242%。这说明「停止」不能是单向的，需要保留末段收尾的设计空间。

**（e）阈值闸门的命中率很低。** 最后一个全量批次里只有 **20%** 的交接时刻满足
`harden_gap_to_next / UB ≤ 5%`（提交 `97ae1e7` 的实测记录）。闸门因此同时是「省成本」和「可能永不交接」的机制。

**（f）全量 A1（`runs/full_a1_cash_late`，`0c29a02`，558×1，SPB 10 s）**：
`CASH` 394/558、`HybridLate` 387/558；524 个交接轮次中 451 轮返回可行解，平均墙钟 214.6 s 对 214.9 s。
证明数仍差 7 —— **当前「何时交接」的默认组合还没有解决主指标。**

### D1.4 候选方案（按建议实现顺序）

| 编号 | 方案 | 要点 |
| --- | --- | --- |
| **S-1** | 进展触发替代轮次计数 | CASH 自己的 UB（或 LB）在最近 X 秒 / 最近一个窗口内没有改善，且剩余预算 ≥ 一个 SPB 窗口，才交接；否则跳过并记录原因 |
| **S-2** | 末段收尾交接 | 停止交接后，在剩余预算约一个窗口处允许最后一次交接，专门覆盖「首轮未中即停止」的尾部事故 |
| **S-3** | 闸门放宽为可调档 | 把 `gate_percent` 从单一 5% 扩成可扫参数；或改为「下 k 个硬化阈值」可达即可（k=2/3） |
| **S-4** | 首轮推迟比例连续化 | `start_fraction ∈ {0.3, 0.5, 0.7}` 扫一遍，并保证「CASH 证明最优立即结束」优先于一切调度 |

**不做的事**：不再把抢占式固定窗口（15/3 或 30/10）作为正式配置。抢占本身就是已确认的损失来源。

### D1.5 判据（可证伪）

- **H1a**：进展触发（S-1）在证明数上不低于纯 CASH，且交接轮次 ≤ `HybridSelective` 的 50%。
- **H1b**：末段收尾（S-2）把「末态 UB 劣于 CASH」的实例数从 17/558 降到 ≤ 5，且不损失证明数。
- **H1c**：闸门（S-3）把 SPB 总耗时再降一半，且证明数下降不超过 1。
- **反例判据**：任何方案若证明数比纯 CASH 低 ≥ 3 且上界配对净负，判定无效，回退到
  「非抢占 + 选择性 + 延后启用」这一最小可用组合。

### D1.6 必需的新记录字段

| 字段 | 含义 |
| --- | --- |
| `handoff_skipped_reason` | `gate` / `progress` / `stopped` / `budget` / `none`，区分「没交接」的四种原因 |
| `handoff_trigger_value` | 触发时的可观测值（如 UB 停滞秒数、`gap/UB`），供离线复盘 |
| `since_last_cash_gain_seconds` | CASH 自己上一次改善 UB/LB 距今的秒数 |
| `tail_handoff` | 本次是否为停止后的末段收尾交接 |

### D1.7 风险

- 闸门/进展条件过严 → 交接永不发生，退化成纯 CASH（可接受但没有贡献），必须靠 `handoff_skipped_reason` 识别。
- 条件过松 → 回到固定窗口的时间税。
- 必须复用已修复的 deadline 语义：每轮用 `arm_cash_deadline()` 重写窗口边界，
  不得回退到 `set_cash_deadline()` 的「只收紧」语义（`1bb0277` 之前的缺陷会让 CASH 在第一次交接后被完全饿死）。

---

## D2：SPB 局部搜索以时间为限，还是以某个 UB 值为目标

### D2.1 设计问题

SPB 当前「跑满窗口再取最好解」。目标通道已经打通
（`improve_with_persistent_weights(assignment, target_cost)` + `spb_target_reached`），
所以真正的问题不是二选一，而是**窗口长度、目标值、轮次三者的组合**。

### D2.2 当前实现

```text
cutoff_time = min(spb_window_seconds, 剩余端到端预算)          # 墙钟上限
target_cost = UB - harden_gap_to_next                         # A1：下一条硬化阈值（raw 单位）
              若阈值不可用则回退到 CASH 当前 LB
达到 target_cost 立即返回，事件记 spb_target / spb_target_reached
```

即：**时间上限是硬约束，目标值是提前返回条件**，目标不得当作硬约束、更不得用于宣布最优。

### D2.3 已有证据

**（a）最好解出现得很早，之后是长时间空跑**（`runs/full23w_time2best`，748 轮，其中 631 轮返回可行解）：

| 统计 | 数值 |
| --- | --- |
| 最好解出现时刻（可行轮次） | 中位 **3.93 s** |
| 最好解出现时刻（被接受轮次） | 中位 **4.88 s** |
| 1 / 2 / 3 / 5 / 8 秒内出现 | 27% / 37% / 43% / 57% / 78% |
| 拿到最好解之后仍空跑的时间 | 中位 **6.07 s**（窗口 10 s） |

**（b）按轮次分层：第 1 轮「早且大」，后续「晚且小」**：

| 轮次 | 中位改善 | 中位出现时刻 |
| --- | --- | --- |
| 第 1 轮 | **21.00%** | **2.04 s** |
| 第 2~3 轮 | 0.78% | 7.32 s |
| 第 4 轮以后 | 0.19% | 9.02 s |

另一个角度：在 251 个上界确实改善过的运行里，SPB 的累计改善只占该运行总改善的**中位 2.1%**。
SPB 的价值主要是**把 CASH 迟早会拿到的界提前拿到**，不是找到 CASH 找不到的解。

**（c）目标几乎从不命中**：

| 目标类型 | 命中次数 |
| --- | --- |
| CASH 下界目标（`350f10f`） | **0 / 107**（Selective 0/65、Late 0/42） |
| A1 硬化阈值目标（`0c29a02`） | 被接受的 24 轮中跨阈 **0/24**，但走了到阈值距离的中位 **33.5%** |
| 阈值闸门可达率 | 只有 **20%** 的交接时刻 `gap/UB ≤ 5%` |

**（d）目标机制当前是零成本保险**：在 50 实例上，A1 阈值目标相对旧版的差异（±1 个证明、2 优 2 劣）
与同批 CASH 自身的漂移量级相同，**测不出净效果**。

### D2.4 设计立场（本文件的结论）

不是「定时间」或「定目标」二选一，而是**上限 + 目标 + 分轮次**：

| 轮次 | 时间上限 | 目标 | 理由 |
| --- | --- | --- | --- |
| 第 1 轮 | 10 s（可试 5 s） | 下一条硬化阈值，达到即返回 | 中位 2 s 就能拿到 21% 的改善，硬跑 10 s 等于送掉 8 s |
| 第 2~3 轮 | **3 s** | 下一条硬化阈值 | 改善 0.78%、中位要到 7.3 s 才出现，用 10 s 不划算 |
| 第 4 轮以后 | 默认不做；满足 D1 的触发条件时才做，2~3 s | 下一条硬化阈值 | 改善 0.19%、中位 9.0 s |
| 预算末段收尾 | 一个窗口 | CASH 当前 LB（命中即最优） | 只在 D1 的 S-2 保留收尾交接时启用 |

注意：目标值只决定「提前返回」，**窗口上限永远保留**，否则目标不可达时就变成了无界搜索。

### D2.5 候选方案与判据

| 编号 | 方案 | 判据 |
| --- | --- | --- |
| **T-1** | `--spb-window 10 / 5 / 3` 三档对照（固定 `HybridSelective` 或 `HybridLate`） | 3 s 档末态 UB 与 10 s 档差距 ≤ 0.2% 量级，且 SPB 总耗时再降 ≥ 50% |
| **T-2** | 目标档位：`LB` / `UB − gap_to_next` / 下 k 个阈值（k=2,3） | 看 `harden_count` 的增长与跨阈次数，而不是只看 UB 改善量 |
| **T-3** | 记录终止原因与返回时的代价 | 新增 `spb_terminated_by ∈ {target, time, budget}`、`spb_cost_at_return`、`spb_target_distance` |

**反例判据**：若 3 s 档证明数下降 ≥ 2 或上界配对净负，则保留 10 s 上限，只在目标机制上做文章。

### D2.6 风险

- 目标不可达时目标机制零收益（当前就是这种状态），必须保留时间上限兜底。
- 目标值必须是 CASH 内部目标单位下、由硬化判据推导出的阈值；
  禁止把 SPB 原始 WCNF 的目标值直接当作内部单位使用（见 `agent.md`「不可放宽的约束」）。
- `SPB 的最优解 ≠ 能证明最优`：`drmx-cryptogen` 两个实例上混合的 LB 已经是最优值 800，
  但 UB 停在 823，CASH 找不到代价 800 的解，仍然证明不了。目标机制要服务于精确侧，而不是自己报一个更好的数字。

---

## D3：启发式的子句分层方案

### D3.1 设计问题

CASH 的一次分层边界同时决定三件事：假设集规模 `n(b)`、本层可换回的权重质量 `M(b)`、
以及当前 `UB − LB` 区间下会被硬化的子句批次。边界切在哪里，直接影响单次 SAT 调用的规模与 LB 推进速度。

### D3.2 当前实现

| 位置（`CASHWMaxSAT-DisjCad-S6/code/uwrmaxsat/MsSolver.cc`） | 内容 | 状态 |
| --- | --- | --- |
| `:278` | `bound = max(lower_bound, W - max(weight_t(1), W/2))` | **唯一生效规则（几何对折）** |
| `:235-264` | `opt_stratification()`：按权重间隙挑最多 10 个切分点 | 整段仍在 `/* */` 注释内 |
| `:809` | 上述函数的唯一调用点 | 已注释 |
| `:816-819` | 初始入口在 `0` 与 `avg_soft_weight` 之间二选一 | 生效 |
| `:318-321` | `harden_soft_cls()` 判据：权重严格大于 `UB − LB` 才硬化 | 生效，是 A1/S 线共用的机制 |

已实现的策略由 `--strat=N` 暴露（提交 `38bf80f`、`dfd4b0e`、`ae4d4c4`）：

| N | 策略 | 边界取法 |
| --- | --- | --- |
| 0 | **P0 几何对折（现状默认）** | `W − max(1, W/2)` |
| 1 | P1 质量均衡 | 使本层承载约一半剩余权重质量 |
| 2 | P2 硬化对齐 | 取不大于 `UB − LB` 的最大权重 |
| 3 | P3 间隙驱动 | 最大相对权重间隙处 |
| 4 | P4 代价模型 | 在数预算内最大化单位假设的权重质量 |
| 5 | S2 在线自适应 | 在 P0 与 P2 之间按上一层 LB 进展在线切换 |

关键修复：第一个层级也必须走策略（`38bf80f`）——难实例在第一个层级里就耗掉大半预算。

### D3.3 语义不变性 Gate：通过

| 路径 | 可比实例 | 最优值不一致 |
| --- | --- | --- |
| CDCL（`exit_reason=optimum`） | 362 | **0** |
| SCIP（`exit_reason=optimum_scip`） | 50 | **1** |

CDCL 路径零不一致，说明分层只改变假设层组织顺序、不改变目标语义。唯一不一致出现在 SCIP 路径：
`ParametricRBACMaintenance_mse20_wt-role_smallcomp_multiple_0.0_2` 在五个策略下给出 88608 / 90880 / 74976 三种值，
且每次都在 1.3~4.6 s 内以 `optimum_scip` 结束、根本没走到分层决策。
**这是与分层无关的 SCIP 通道一致性问题，修复前不得用 SCIP 路径的读数支持分层结论。**

### D3.4 结果矩阵

证明数（越大越好）：

| 策略 | 调试集 50 | 全量 558（种子 20260909） | 全量 558（种子 20260910） |
| --- | --- | --- | --- |
| **P0** | **36** | **379** | **378** |
| P1 | 35 | 373 | — |
| P2 | 34 | 374 | 371 |
| P3 | 34 | 377 | — |
| P4 | 33 | 373 | — |
| S2 | 33~34 | — | — |

末态 UB 配对（相对 P0，全量，种子 20260909）：

| 策略 | 更优 / 更差 | 更优中位改善 |
| --- | --- | --- |
| P1 | 65 / 32 | −14.46% |
| **P2** | **82 / 40** | **−11.43%** |
| P3 | 29 / 34 | −1.19% |
| P4 | 78 / 47 | −16.32% |

P2 在第二种子上复现（81 / 44）。收益按实例族高度集中：
`generalized-ising` +15、`decision` +13、`correlation-clustering` +10/2、`ramsey` +4、
`railway-transport` +4、`judgment` +5/5、`hs-timetabling` +4/3。

### D3.5 机制结论

- **H2 被证伪**：P2 的 82 个受益实例中发生软子句硬化的为 **0** 个——收益不来自硬化阈值。
  收益集中在少数权重结构特殊的实例族上，机制尚未解释。
- **S2 在线控制器回收了 66% 的 oracle 收益**（oracle 58.50%，S2 实得 38.60%），高于 50% 门槛。
- **P2 不与混合协议叠加**：`HybridLate + P2` 证明 34/50（P0 版 36/50），末态 UB 1 优 / 43 平 / 4 劣。
  P2 的定位因此是「**纯 CASH、解质量优先**」配置。

### D3.6 候选方案

| 编号 | 方案 | 要点 |
| --- | --- | --- |
| **C-1** | 特征化策略选择器 | 静态特征（不同权值数、最大/最小比、权重分位数、间隙熵、BMO 点、硬子句占比）+ 运行期特征（第一层 ΔLB / 耗时），离线学习「P0 vs P2」，再上线 |
| **C-2** | 在线控制器升级 | S2 目前是两臂、以 LB 进展为奖励；扩到三臂，并把奖励改成「单位时间 LB 进展」 |
| **C-3** | 第一层与后续层分开调参 | 第一层已走策略，但 P0/P2 的差别需按层统计，避免被第一层的固定开销掩盖 |
| **C-4** | 与混合解耦 | D3 只在纯 CASH 配置上作为候选贡献，不与 D1/D2 叠加（交界消融已给出负向结论） |

### D3.7 判据

- **H1**：存在实例子集使某个非 P0 策略显著优于 P0（证明数或 anytime gap）。
- **H3**：在线控制器在零额外预算下收回事后最优收益的 ≥ 50%。
- **H4**：结论按权重结构特征分层后仍成立（不能只在少数族上成立）。
- **C-1 判据**：在未参与拟合的实例族与第二种子上仍净正。
- 若 C-1 在两种子上都不显著，则退回「**P0 默认 + P2 作为解质量配置**」，D3 降级为讨论章节。

### D3.8 必需的新可观测

每层记录 `boundary`、层内权重区间、`n(b)`、层前后 LB、本层耗时、本层新硬化子句数；
离线统计一次权重分布特征（分位数、不同权值数、间隙熵、最大最小比、有无 BMO 点）。
没有这些字段就只能报总体数字，无法解释胜负来源。

### D3.9 风险

- `MSE24W` 当前不可读，只能用 `MSE23W`，外部效度受限。
- SCIP 路径存在与分层无关的不确定性，必须先把两种读数分开。
- 收益族集中，小样本上极易过拟合。

---

## 4. 三点之间的关系

```text
                     ┌──────────────────────────────┐
   精确侧 ────────────│ D3 分层边界（MsSolver 内部） │
                     └──────────────────────────────┘
                                  │ 共用同一个量：UB − LB
                                  ▼
   ┌────────────────────────────────────────────────────┐
   │ 硬化阶梯：harden_gap_to_next / harden_count        │
   └────────────────────────────────────────────────────┘
        ▲                                   ▲
        │ D1 用它当交接闸门                  │ D2 用它当 SPB 的目标值
   ┌────────────┐                      ┌────────────┐
   │ 何时交接   │ ──── 交接成本 ────▶  │ 窗口跑多久 │
   └────────────┘                      └────────────┘
```

- D1 与 D2 共用 `harden_gap_to_next`：必须在**同一时刻、同一单位**取快照（回调快照缓存 `ef97202` 已提供）。
- D2 是 D1 的成本模型：D1 决定要不要付，D2 决定付多少。
- D3 独立于 D1/D2，已验证不与混合叠加；可与 D1/D2 并行推进，但不要混在同一批次里。
- 三者共用一个判据来源：`MsSolver.cc:318-321` 的硬化不等式。任何改动都不得修改这一判据本身。

## 5. 建议的实验队列

| 顺序 | 批次名（建议） | 配置 | 数据与参数 | 目的 |
| --- | --- | --- | --- | --- |
| 1 | 收完 `full_gate_phase` | `CASH,HybridGate,HybridPhase` | 正在运行；SPB 5 s | D1 闸门与 D2 相位通道的全量读数 |
| 2 | `spbwin_3_5_10` | `CASH,HybridLate` × 窗口 3/5/10 s | `debug50` 先跑，再全量 | D2 的 T-1：量出第 4~10 秒的边际价值 |
| 3 | `a1_targets` | `HybridLate` × 目标档位 | `debug50` | D2 的 T-2：LB / 下 k 个阈值哪一档真能跨阈 |
| 4 | `hybrid_progress` | `CASH,HybridProgress` | `debug50` → 全量 558 | D1 的 S-1：进展触发 |
| 5 | `hybrid_tail` | `CASH,HybridTail` | `debug50` | D1 的 S-2：末段收尾 |
| 6 | 离线分析（不占预算） | — | `runs/full23w_strat_*`、`runs/seed2_strat_*` | D3 的 C-1：策略选择器特征与标签 |

每个批次开始前必须满足 `experiment.md` §10 的 Gate 与「实验必须绑定提交」：
工作树干净、`git rev-parse HEAD` 与二进制 sha1 写入 `meta.json`、并发 ≤ 64。

## 6. 状态维护规则

1. 每完成一个实验，把过程与原始读数写进 `docs/experiments/<日期>-<主题>.md`。
2. 只把**结论、判据与状态**回写到本文件对应的「判定状态 / 下一步」两列，不在本文件堆日志。
3. 任何被判无效的方案都要在本文件留下记录和反例理由，避免重复提出。
4. 本文件与 `agent.md` / `experiment.md` / `plan.md` 冲突时，以后者为准，并立刻修正本文件。

## 7. 数据与回溯来源

```text
docs/experiments/2026-09-11-module-effects.md          逐模块量化配对（本文主要数字来源）
docs/experiments/2026-09-11-selective-handoff.md       轮次衰减、选择性交接
docs/experiments/2026-09-11-lb-target-and-late-start.md 下界目标、延后启用
docs/experiments/2026-09-11-spb-value-distribution.md  最好解时刻、时间 vs 目标
docs/experiments/2026-09-11-hardening-check.md         硬化阈值与 SPB 上界
docs/experiments/2026-09-11-lb-ub-trajectory.md        实测 LB/UB 逐轮轨迹
docs/experiments/2026-09-11-deadline-starves-cash.md   deadline 只收紧缺陷
docs/experiments/2026-09-11-stratification-policies.md 分层策略完整矩阵
docs/planning/2026-09-11-stratification-boundary.md    S 线设计
docs/planning/2026-09-11-Aline-innovation.md           A 线设计

服务器批次：runs/full23w_fix、runs/debug50_selective、runs/full23w_selective、
            runs/debug50_target_late、runs/debug50_harden、runs/full23w_time2best、
            runs/debug50_harden、runs/strat2_p0..p5、runs/full23w_strat_p0..p4、
            runs/seed2_strat_p0/p2、runs/late_strat0/2、runs/a1_debug50、
            runs/full_a1_cash_late、runs/full_gate_phase（运行中）
```

---

## 版本记录

| 日期 | 版本 | 内容 |
| --- | --- | --- |
| 2026-09-12 | v1 | 建立本文件：三点改进（交接时机 / SPB 终止条件 / 分层方案），汇总截至 HEAD `97ae1e7` 的证据、判据与实验队列 |
